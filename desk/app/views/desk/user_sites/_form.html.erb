<%= form_with(model: user_site, local: true) do |form| %>
  <% if user_site.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_site.errors.count, "error") %> prohibited this user_site from being saved:</h2>

      <ul>
      <% user_site.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.collection_select :user_id, User.all, :id, :name %>
  </div>

  <div class="field">
    <%= form.label :site_id %>
    <%= form.collection_select :site_id, Site.all, :id, :domain %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
